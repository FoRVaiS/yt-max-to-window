{
  "name": "yt-max-to-window",
  "version": "1.0.0",
  "dependencies": {
    "jquery": "^3.3.1"
  },
  "devDependencies": {
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.2.3",
    "babel-core": "^7.0.0-bridge.0",
    "babel-jest": "^23.6.0",
    "eslint": "^5.12.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.14.0",
    "jest": "^23.6.0",
    "parcel-bundler": "^1.11.0",
    "rimraf": "^2.6.3"
  },
  "license": "MIT",
  "main": "src/index.js",
  "scripts": {
    "build": "npm run clean && parcel build src/index.js --no-source-maps --bundle-node-modules",
    "clean": "rimraf dist",
    "test": "jest",
    "test:watch": "jest --watchAll"
  }
}
